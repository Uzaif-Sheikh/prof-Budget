import { createTheme } from '@mui/material/styles';

const Theme = createTheme({
    palette: {
        primary: {
            main: "#232428",
            dark: "#1f1f23"
        },
        secondary: {
            main: "#27282D"
        }
    }
});


export default Theme;

